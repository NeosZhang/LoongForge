[project]
name = "LoongForgeVLA"
version = "0.1.0"
description = "composable action and condition for VLA training infra"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.1.0",
    "numpy>=1.24.0",
    "omegaconf>=2.3.0",
    "accelerate>=0.25.0",
]

[project.optional-dependencies]
dev = ["pytest>=7.0", "pytest-cov"]
